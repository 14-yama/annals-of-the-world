---
title: Jelton Mentore — Curator To-Do Log
owner: Jelton Mentore
status: active
created: 2025-10-10
---

# Jelton Mentore — Curator To-Do Log

This is a lightweight, chronological curator log for housekeeping tasks and
curation checklists. Use the `- [ ]` checklist lines to mark progress. Keep
entries short and reference the relevant doc/seed files.

## First checklist: Populate Hebrew cluster — Cite stage (priority)

Goal: Complete the Hebrew cluster cite stage with an even spread of nodes
across Timeframes (910–960). Prioritize creating reusable Evidence nodes and
linking existing proposed triples to those Evidence nodes.

Checklist

- [ ] Audit current `docs/guidelines/hebrew_cluster.md` and extract all inline
  evidence strings (e.g., `cite:920-ev1`) into a CSV of candidate Evidence
  nodes (`evidence_candidates.csv`).
- [ ] Seed `Evidence` nodes for high-reuse sources (Dead Sea Scrolls, Leningrad
  Codex, Septuagint, Ketef Hinnom amulets, Elephantine Papyri, DJD editions)
  as Tier A or B in `nodes_class9_evidence.csv`.
- [ ] For each Timeframe (910–960), ensure at least 8 proposed nodes across
  types (Ideas, People, Events, Texts, Institutions, Evidence). Create missing
  nodes as `PROPOSED` in `nodes_class9.csv`.
- [ ] Generate `rels_class9.csv` linking new nodes using canonical verbs from
  `docs/guidelines/relations_vocabulary.md`. Mark each edge with an
  `evidence_slug` referencing seeded Evidence nodes.
- [ ] Run `scripts/normalize_verbs.py` (create if missing) to report any
  non-canonical verbs found in `rels_class9.csv`.
- [ ] Run `scripts/run_audits.py` (after installing `neo4j` driver & securing
  credentials) to detect missing `FRAMED_BY` edges and orphan nodes; export
  report to `reports/audit-YYYYMMDD.json`.
- [ ] Promote inline citation strings used on ≥2 edges to `:Evidence` nodes and
  update `rels_class9.csv` to point to the Evidence slug instead of inline
  text.
- [ ] Create a short PR with seed CSVs + a `docs/guidelines/hebrew_cluster_seed.md`
  describing source selection and curation criteria.

## Quick links

- Hebrew cluster doc: `docs/guidelines/hebrew_cluster.md`
- Relations vocabulary: `docs/guidelines/relations_vocabulary.md`
- Audit queries: `docs/guidelines/audit_queries.md`
- Audit runner: `scripts/run_audits.py`

## Notes & assumptions

- Assume the `neo4j` Python driver will be available in the environment for
  audit runs; otherwise install via pip in the configured environment.
- Use kebab-case slugs for new nodes.
- Evidence tiers: A (Primary), B (Peer-reviewed), D (Institutional/catalog).

## Done items

- [ ] (placeholder) initial population of high-use evidence nodes in nodes CSV


---

Generated by automation on 2025-10-10
